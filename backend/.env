# MongoDB Connection String
MONGODB_URI=mongodb://localhost:27017/webadmin_flutter
# For MongoDB Atlas (cloud): mongodb+srv://username:password@cluster.mongodb.net/webadmin_flutter

# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:8081,http://localhost:3000,http://127.0.0.1:8081

# Optional: JWT Secret for authentication (future use)
JWT_SECRET=your_jwt_secret_key_here
